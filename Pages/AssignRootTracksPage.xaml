<Page x:Class="WPFBMI.AssignRootTracksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFBMI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AssignRootTracksPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <CheckBox x:Name="OnlyUnassignedButton" Content="Show only Unassigned" Click="UnassignedButtonHandler" Grid.Column="1" Grid.Row="1"/>
        <Button x:Name="AutoAssignButton" Content="Auto-Assign" Click="DoAutoAssignment" Grid.Column="2" Grid.Row="1"/>
        <Button x:Name="ExitButton" Content="Go Back" Click="GoBack" Grid.Column="3" Grid.Row="1"/>
        <DataGrid x:Name="TrackGrid" ItemsSource="{Binding Tracks}" AutoGenerateColumns="False" Grid.Column="0" Grid.ColumnSpan="4" Grid.Row="2"
                  CanUserAddRows="False" CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Usage ID" Binding="{Binding id}" IsReadOnly="True" />
                <DataGridTextColumn Header="Usage Track Title" Binding="{Binding title_name}" IsReadOnly="True" />
                <DataGridTextColumn Header="Root ID" Binding="{Binding root_id}" IsReadOnly="True" />
                <DataGridTextColumn Header="Root Track Title" Binding="{Binding root_name}" Width="100" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Assign/Reassign">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Assign" 
                            Click="AssignOrReassignTrack" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
